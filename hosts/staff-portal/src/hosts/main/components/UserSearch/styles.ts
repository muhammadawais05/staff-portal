import { css } from 'styled-components'

// PS. These styles are needed because overflow ellipsis only work within block elements
export const searchItemLink = css`
  width: 100%;
  display: block;
`
