import { css } from 'styled-components'

export const betaSwitcherContainer = css`
  position: fixed;
  bottom: 0;
  right: 0;
  z-index: 1;
`

export const betaSwitcherButton = css`
  position: absolute;
  bottom: 1rem;
  right: 1rem;
`
