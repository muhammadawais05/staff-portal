import { css } from 'styled-components'
import { palette } from '@toptal/picasso/utils'

export const withDefaultCursor = css`
  cursor: default;
  color: ${palette.blue.main};
`

export const titleTagsContainer = css`
  position: absolute;
  top: 2.2rem;
`
