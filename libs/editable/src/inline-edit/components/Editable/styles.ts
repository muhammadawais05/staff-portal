import { palette } from '@toptal/picasso/utils'
import { css } from 'styled-components'

export const editButton = css`
  color: ${palette.grey.dark};
`
export const editableViewerWidth = css`
  min-width: 0;
`
