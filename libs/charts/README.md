# Charts

This package contains components, types and services that relates to charts. It
also serves as a proxy for `@toptal/picasso-charts` and
`@topkit/analytics-charts` packages. If you want to use something from packages
above it should be reexported here. Direct usages of these two packages are
restricted.
