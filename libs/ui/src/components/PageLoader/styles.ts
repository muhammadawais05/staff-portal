export const container = `
  pointer-events: none;
  user-select: none;
  position: relative;
  height: 100vh;
`

export const centeredLoader = `
  position: absolute;
  transform: translate(-50%, -50%);
  left: 50%;
  top: 50%;
`
