import { css } from 'styled-components'
import { palette } from '@toptal/picasso/utils'

export const mediumFilterButton = css`
  margin-top: 0.25rem;
`

export const filtersFormWrapper = css`
  background: ${palette.grey.lighter};
  border-radius: 0.5em;
`
