import { css } from 'styled-components'

export const content = css`
  padding-bottom: 3rem;
`

export const actionButtons = css`
  position: absolute;
  bottom: 0;
  width: 100%;
  left: 0;
  padding-top: 2rem;
  background: linear-gradient(0deg, white 70%, rgb(255 255 255 / 0%) 100%);
`
