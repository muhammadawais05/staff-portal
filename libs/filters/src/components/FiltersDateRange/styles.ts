import { css } from 'styled-components'

/**
 * To properly align date range fields with other two column rows
 * 9rem is the spacing allocated for left aligned labels
 */
export const filtersDateRangeGrid = css`
  padding-right: 9rem;
`
