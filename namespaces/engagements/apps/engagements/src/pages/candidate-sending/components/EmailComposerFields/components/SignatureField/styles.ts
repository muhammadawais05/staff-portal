import { css } from 'styled-components'

export const signatureField = css`
  & * {
    box-sizing: content-box;
    position: relative;
    padding: 0;
    margin: 0;
    vertical-align: inherit;
    text-align: inherit;
    line-height: initial;
  }
`
