import { css } from 'styled-components'

export const cellCheckbox = css`
  && {
    width: 2.5rem;
  }
`

export const rowHeaderScrollable = css`
  && {
    position: sticky;
    top: 0;
    z-index: 9999;
    background-color: #fff;
  }
`
