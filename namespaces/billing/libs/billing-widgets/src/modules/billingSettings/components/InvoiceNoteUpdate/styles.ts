import { palette } from '@toptal/picasso/utils'
import { css } from 'styled-components'

export const background = css`
  background-color: ${palette.grey.lightest};
  border-top: solid 1px ${palette.grey.lighter2};
  padding-top: 1rem;
`
