import { palette } from '@toptal/picasso/utils'
import { css } from 'styled-components'

const tableExclamationIcon = css`
  && {
    margin-left: 0.4rem;
    vertical-align: -0.325em;
    color: ${palette.grey.light};
  }
`

export { tableExclamationIcon }
