import { css } from 'styled-components'

const tableConsolidatedTooltip = css`
  display: inline-block;
`

const tableConsolidatedIcon = css`
  && {
    margin-left: 0.2rem;
    vertical-align: -0.325em;
  }
`

export { tableConsolidatedTooltip, tableConsolidatedIcon }
