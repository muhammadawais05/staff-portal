import { css } from 'styled-components'

const dateCell = css`
  && {
    white-space: nowrap;
  }
`

const detailsCell = css`
  &&& {
    max-width: 17.8rem;
  }
`

const menuCell = css`
  &&& {
    padding-right: 1rem;
    width: 7.1rem;
  }
`

export { dateCell, detailsCell, menuCell }
