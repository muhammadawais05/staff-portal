import { css } from 'styled-components'

const recipientWidth = '22rem'
const statusWidth = '10rem'

const recipient = css`
  width: ${recipientWidth};
`

const status = css`
  width: ${statusWidth};
`

const message = css`
  width: 100%;
`

export { recipient, status, message }
