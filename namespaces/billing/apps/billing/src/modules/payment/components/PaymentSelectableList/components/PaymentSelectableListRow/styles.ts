import { css } from 'styled-components'

const rowCellAmount = css`
  && {
    text-align: right;
  }
`

const descriptionWrapper = css`
  && {
    flex-grow: 1;
    width: 100%;
  }
`

export { rowCellAmount, descriptionWrapper }
