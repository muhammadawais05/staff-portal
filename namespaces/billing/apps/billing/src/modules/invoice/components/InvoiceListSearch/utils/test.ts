// import { FilterConfigType } from '@staff-portal/filters'

describe('buildInvoiceDropdownFilter', () => {
  // TODO: Staff Portal recent changes breaks tests
  // eslint-disable-next-line
  it.todo('works properly')
  // , () => {
  //   const name = 'client_partner_id'
  //   const fn = jest.fn().mockReturnValue({ options: [], loading: false })

  //   expect(buildInvoiceDropdownFilter(name, fn)()).toEqual({
  //     label: 'Enterprise client partner',
  //     loading: false,
  //     name: 'client_partner_id',
  //     options: [{ label: 'Not selected', value: null }],
  //     type: FilterConfigType.SELECT
  //   })
  // })
})
