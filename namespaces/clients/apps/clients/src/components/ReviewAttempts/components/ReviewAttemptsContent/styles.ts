import { css } from 'styled-components'

export const table = css`
  table-layout: fixed;
`

const baseWidth = '15rem'

export const dateCol = css`
  width: ${baseWidth};
`
export const resultCol = css`
  width: ${baseWidth};
`
