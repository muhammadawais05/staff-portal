export const MIN_RESTRICTION_DAYS = 1

export enum ActivationStatus {
  Active = 'active',
  InProgress = 'in_progress',
  Paused = 'paused',
  Rejected = 'rejected'
}

export enum ActivationTypes {
  ACTIVATION = 'activation',
  SCREENING = 'screening'
}
