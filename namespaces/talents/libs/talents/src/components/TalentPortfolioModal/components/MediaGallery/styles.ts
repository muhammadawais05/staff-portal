import { css } from 'styled-components'

export const galleryContainer = css`
  position: relative;
  width: 100%;
  height: 100%;
`

export const mainImageContainer = css`
  position: absolute;
  bottom: 6.785rem;
  left: 1.5rem;
  right: 1.5rem;
  top: 1.5rem;
`
export const thumbnailsContainer = css`
  position: absolute;
  bottom: 1.5rem;
  left: 1.5rem;
  right: 1.5rem;
`

export const thumbnailsWrapper = css`
  flex-wrap: nowrap;
`
