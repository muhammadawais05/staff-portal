import { css } from 'styled-components'

// Picasso makes table expandable content font size smaller, this restores the default font sizes
export const jobApplicantContent = css`
  font-size: 1rem;
`

export const talentName = css`
  flex-grow: 1;
`
