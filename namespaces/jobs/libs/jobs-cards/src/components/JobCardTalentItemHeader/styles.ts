import { css } from 'styled-components'
import { palette } from '@toptal/picasso/utils'

export const icon = css`
  margin-left: 0.5rem;
  color: ${palette.grey.dark};
`

export const slackLink = css`
  color: black;
`
