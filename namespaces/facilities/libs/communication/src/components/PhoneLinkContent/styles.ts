import { css } from 'styled-components'

export const phoneLink = css`
  height: auto;
  display: flex;
  background-color: transparent;
  font-size: inherit;
  user-select: text;

  && > span {
    font-size: inherit;
    line-height: 1.5em;
  }
`
