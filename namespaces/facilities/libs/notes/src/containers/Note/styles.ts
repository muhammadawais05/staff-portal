import { css } from 'styled-components'
import { palette } from '@toptal/picasso/utils'

export const archivedNoteContent = css`
  border: 1px solid ${palette.grey.main};
  background-color: ${palette.grey.lighter};
`
