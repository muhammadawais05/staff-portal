import { palette } from '@toptal/picasso/utils'
import { css } from 'styled-components'

export const noteContent = css`
  padding-top: 0.5rem;
`

export const groupedAnswer = css`
  padding-top: 1.5rem;
  margin-bottom: 1.5rem;
  border-top: 1px solid ${palette.grey.light2};
`
