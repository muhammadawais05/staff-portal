import { TaskCardType } from '@staff-portal/tasks-cards'

export const TASK_CARD_PRIORITY: TaskCardType[] = [
  TaskCardType.Job,
  TaskCardType.Invoice,
  TaskCardType.Payment,
  TaskCardType.CommunityEvent,
  TaskCardType.RateChangeRequest,
  TaskCardType.Talent,
  TaskCardType.Company,
  TaskCardType.TaskDetails,
  TaskCardType.Activity
]
