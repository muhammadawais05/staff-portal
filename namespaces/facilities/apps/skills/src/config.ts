import {
  OrderDirection,
  SkillNameOrderField
} from '@staff-portal/graphql/staff'

export const DEFAULT_SORT_FIELD = SkillNameOrderField.SKILL

export const DEFAULT_SORT_ORDER = OrderDirection.ASC

export const PAGE_SIZE = 20
