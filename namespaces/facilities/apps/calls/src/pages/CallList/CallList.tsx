import React from 'react'

import CallTablePage from '../../components/CallTablePage'

const CallList = () => {
  const pageTitle = 'Call History'

  return <CallTablePage pageTitle={pageTitle} />
}

export default CallList
