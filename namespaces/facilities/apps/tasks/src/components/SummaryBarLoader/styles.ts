import { palette } from '@toptal/picasso/utils'
import { css } from 'styled-components'

export const overviewBlocks = css`
  border: 1px solid ${palette.grey.light};
  border-radius: 0.5rem;
`

export const paragraphSmall = css`
  width: 50%;
`
