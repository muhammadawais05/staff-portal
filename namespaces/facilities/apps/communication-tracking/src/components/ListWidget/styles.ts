export const disabledContainer = `
  pointer-events: none;
  opacity: 0.4;
  user-select: none;
  position: relative;
`

export const loadingIndicator = `
  position: absolute;
  transform: translateX(-50%);
  left: 50%;
  top: 10rem;
`
